'use client';
import { useState } from 'react';
export default function PatientLoginPage() { const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(''); const submit = async (event: React.FormEvent) => { event.preventDefault(); setError(''); const response = await fetch('/api/paciente/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email, password }) }); const data = await response.json(); if (data.success) window.location.href = '/mi-agenda'; else setError(data.error); }; return <main style={{ minHeight: '100vh', background: '#f0f9fa', display: 'grid', placeItems: 'center', padding: 20 }}><form onSubmit={submit} style={{ width: '100%', maxWidth: 410, background: 'white', padding: 36, borderRadius: 24, boxShadow: '0 20px 60px #0f172a18' }}><p style={{ color: '#0e7490', fontWeight: 800 }}>PORTAL DEL PACIENTE</p><h1 style={{ color: '#0f172a' }}>Mis sesiones</h1><p style={{ color: '#64748b' }}>Ingresa para ver y gestionar tus citas.</p><input required type="email" placeholder="Correo electrónico" value={email} onChange={e => setEmail(e.target.value)} style={{ width: '100%', boxSizing: 'border-box', padding: 14, marginTop: 16, borderRadius: 10, border: '1px solid #cbd5e1' }}/><input required type="password" placeholder="Contraseña" value={password} onChange={e => setPassword(e.target.value)} style={{ width: '100%', boxSizing: 'border-box', padding: 14, marginTop: 12, borderRadius: 10, border: '1px solid #cbd5e1' }}/>{error && <p style={{ color: '#dc2626' }}>{error}</p>}<button style={{ width: '100%', marginTop: 18, padding: 14, border: 0, borderRadius: 10, background: '#0891b2', color: 'white', fontWeight: 800 }}>Ingresar</button></form></main>; }
