import prisma from '@/lib/db'; import { hashPatientPassword } from '@/lib/auth/patient-session'; import { randomBytes } from 'crypto';
export async function ensurePatientAccount(email:string){const normalized=email.toLowerCase().trim(),old=await prisma.patientAccount.findUnique({where:{email:normalized}});if(old)return null;const booking=await prisma.booking.findFirst({where:{email:{equals:normalized,mode:'insensitive'}},orderBy:{createdAt:'desc'}});const temporary=`Portal-${randomBytes(7).toString('base64url')}!`;await prisma.patientAccount.create({data:{email:normalized,passwordHash:hashPatientPassword(temporary),mustChangePassword:true,rut:booking?.rut||null,firstName:booking?.firstName||null,secondName:booking?.secondName||null,firstSurname:booking?.firstSurname||null,secondSurname:booking?.secondSurname||null,address:booking?.address||null,region:booking?.region||null,commune:booking?.commune||null,phone:booking?.phone||null}});return temporary;}
